\begin{enumerate}
\item
$P = x(x+3)(2x-3)$とする．
また，$a$を定数とする．
$x = a+1$のときの
$P$の値は
\begin{equation*}
2a^3+\fbox{  ア  }a^2+\fbox{  イ  }a-\fbox{  ウ  }
\end{equation*}
である．

$x=a+1$のときの$P$の値と，$x=a$のときの$P$の値が等しいとする．このとき，$a$は

